# TASKS — Hermes 控制台全面重构

> 从 spec.md + plan.md 拆解出的具体可执行任务。
> 完成后标记 ✅，新增任务追加到对应阶段。

## 阶段 1: 基础框架 + 仪表盘（P0）

- ✅ T1: 修复首次加载问题 — 移除移动端弹窗 bug，确保 PC/移动端直接进入仪表盘
  - **问题**：index.html 中 Vue 模板语法 `{{ confirmIcon }}` 等在 Vue 未加载时显示原始文本
  - **修复**：给确认弹窗和 Toast 容器添加 `v-cloak` 指令 + CSS `[v-cloak] { display: none }`；确保 `showConfirm` 初始为 `false`
  - **涉及文件**：index.html、css/main.css
  - **验证**：首次访问（清缓存）PC 和移动端均无弹窗

- ✅ T2: 修正官方控制台链接 — 指向 http://192.168.5.110:19119/login
  - **问题**：app.js 中 officialConsoleUrl 指向绿联云
  - **修复**：修改为 http://192.168.5.110:19119/login
  - **涉及文件**：js/app.js
  - **验证**：点击"官方控制台"按钮正确跳转

- ✅ T3: 重构仪表盘 UI — Linear 风格卡片布局
  - **问题**：已配置模型/连接渠道/最近日志区域无设计
  - **修复**：
    - 重新设计 stat-grid 卡片（图标+数值+标签）
    - 重新设计 info-grid 三栏布局（模型/渠道/日志）
    - 渠道显示中文名（feishu→飞书）+ 正确图标
    - 日志条目带级别颜色标签
  - **涉及文件**：index.html、css/main.css、js/app.js
  - **验证**：仪表盘各区域样式美观、数据正确

- ✅ T4: 浅色/深色主题基础适配 — CSS 变量体系
  - **问题**：深色模式图表消失，浅色模式文字不可见
  - **修复**：
    - 建立 CSS 变量体系（--bg-primary、--text-primary 等）
    - 浅色主题为默认（纯白 #ffffff 背景、#171717 文字）
    - 深色主题为可选（#0a0a0a 背景、#ededed 文字）
    - ECharts 图表颜色跟随主题
    - 所有文字颜色使用变量
  - **涉及文件**：css/main.css、js/app.js
  - **验证**：默认浅色主题所有内容可见，切换深色后图表正常

## 阶段 2: 会话管理（P0）

- ✅ T5: 重构会话列表 UI — 表格样式 + 完整 ID 显示
  - **问题**：ID 截断、样式丑
  - **修复**：
    - 表格使用 Linear 风格（细边框、hover 高亮）
    - ID 使用等宽字体，不截断（或 tooltip 显示完整）
    - 添加分页控件
    - 搜索框样式优化
  - **涉及文件**：index.html、css/main.css
  - **验证**：会话列表美观、ID 完整可见

- ✅ T6: 实现会话详情页 — 消息历史展示
  - **问题**：详情页白底白字无样式
  - **修复**：
    - 创建会话详情视图（替换当前空壳）
    - 消息气泡样式（用户/AI 区分）
    - 时间戳显示
    - 返回按钮
  - **涉及文件**：index.html、css/main.css、js/app.js、serve.py（后端 API）
  - **验证**：点击查看会话显示消息历史，样式正常

- ✅ T7: 实现会话编辑功能 — 重命名标题
  - **问题**：无法编辑会话
  - **修复**：
    - 添加编辑按钮（每行操作列 ✏️ 图标）
    - 点击后标题变为可编辑输入框（inline-edit 样式）
    - Enter 保存、Escape 取消、✓/✕ 按钮
    - 保存时调用后端 PUT API
    - 后端添加 PUT /api/sessions/:id 接口 + update_session_title 函数
  - **涉及文件**：index.html、js/app.js、js/api.js、css/main.css、serve.py
  - **验证**：可以重命名会话标题，编辑状态有蓝色边框高亮，保存后立即更新

- ✅ T8: 实现会话删除功能 — 确认弹窗
  - **问题**：删除无确认
  - **修复**：
    - 删除前弹出确认弹窗（使用已有 confirm dialog 基础设施）
    - 显示会话标题确认（confirmDeleteSession 函数）
    - 确认后调用删除 API
    - 弹窗图标 🗑️、标题「删除会话」、显示会话名称
  - **涉及文件**：index.html、js/app.js
  - **验证**：删除有确认弹窗，确认后会话消失，计数更新

## 阶段 3: 定时任务（P0）

- ✅ T9: 实现定时任务列表 — 读取并展示 cron 数据
  - **问题**：页面空白
  - **修复**：
    - 后端 /api/cron 正确读取 cron.json 或通过 Gateway API 获取
    - 前端展示任务列表（名称、计划、状态、最后执行时间）
    - 空状态显示引导文案
  - **涉及文件**：serve.py、js/app.js、index.html
  - **验证**：定时任务页面显示现有任务

- ✅ T10: 实现创建定时任务 — 表单弹窗
  - **问题**：无法创建任务（POST /api/cron 被 GET handler 拦截）
  - **修复**：
    - 修复 serve.py 路由 bug：`elif path == '/api/cron':` → `elif path == '/api/cron' and method == 'GET':`（原 GET handler 不检查 method，导致 POST 请求也被匹配到 GET 路由）
    - 前端和 API 层已有完整实现（表单弹窗、提交逻辑、后端 create_cron_job 函数）
  - **涉及文件**：serve.py（1 行路由修复）
  - **验证**：POST /api/cron 正确创建任务，前端表单弹窗完整可用（名称、调度方式、prompt、投递渠道、启用开关）

- ✅ T11: 实现编辑/删除/执行定时任务
  - **问题**：无编辑/删除/执行功能
  - **修复**：前端、API、后端均已实现
    - 编辑按钮 → `openCronForm('edit', job)` 预填弹窗，保存时 delete+create
    - 删除按钮 → `confirmDeleteCron(job)` 确认弹窗 + `deleteCron(id)`
    - 立即执行按钮 → `runCron(id)` 调用 POST /api/cron/:id/run
    - 暂停/启用按钮 → `toggleCron(job)` 调用 POST /api/cron/:id/toggle
  - **涉及文件**：index.html、js/app.js、js/api.js、serve.py（全部已就绪）
  - **验证**：编辑弹窗预填数据正确、删除有确认弹窗、立即执行调用 API、暂停/启用切换状态

## 阶段 4: 模型配置 + 消息渠道（P0/P1）

- ✅ T12: 重构模型配置页 — 下拉选择 + 验证
  - **问题**：手动输入模型名易错、UI 极差
  - **修复**：
    - 提供商下拉选择（从 config 读取已有提供商）
    - 模型名称下拉或输入（带自动完成）
    - 切换前验证模型名称
    - 提供商列表卡片样式
    - 添加/编辑/删除提供商功能
  - **涉及文件**：index.html、css/main.css、js/app.js、serve.py
  - **验证**：通过下拉选择切换模型，不会因拼写错误失败

- ✅ T13: 重构消息渠道页 — 中文名 + 图标 + 状态
  - **问题**：显示拼音、图标不明、状态错误
  - **修复**：
    - 渠道中文名映射（feishu→飞书、telegram→Telegram、discord→Discord）
    - 正确图标（飞书用飞书 logo、Telegram 用纸飞机等）
    - 连接状态从 Gateway API 正确获取
    - 渠道管理功能（查看配置）
  - **涉及文件**：index.html、css/main.css、js/app.js
  - **验证**：飞书显示"飞书"中文名 + 正确图标 + "已连接"状态

## 阶段 5: 技能/插件管理（P1）

- ✅ T14: 重构技能管理页 — 卡片网格 + 搜索
  - **问题**：UI 差、只能查看
  - **修复**：
    - 卡片网格布局（Linear 风格）
    - 搜索筛选（名称、描述）
    - 技能详情弹窗
    - 分类标签
  - **涉及文件**：index.html、css/main.css
  - **验证**：技能列表美观、搜索可用

- ✅ T15: 重构插件管理页 — 分类 + 详情
  - **问题**：UI 差、功能少
  - **修复**：
    - 分类筛选标签（MCP/工具集/平台工具集）— 用 tab 式按钮替代下拉菜单
    - 卡片详情弹窗（类型、状态、配置、来源、传输方式）
    - 搜索功能（保留原有）
    - 启用/禁用切换开关（toggle switch）
    - 新卡片设计：图标色块包装、名称+badge、描述、底部状态标签
  - **涉及文件**：index.html、css/main.css、js/app.js
  - **验证**：筛选 tab 正确过滤、详情弹窗完整、toggle 开关可操作、卡片 hover 动效正常

## 阶段 6: 系统设置 + 监控 + 日志（P1）

- ✅ T16: 重构系统设置页 — 分类 + 说明
  - **问题**：配置项少、无说明
  - **修复**：
    - 分类展示（智能体、显示、审批三大分区）
    - 每个配置项添加中文说明 hint
    - 新增设置：推理深度、详细日志、紧凑模式、流式输出、显示费用、显示推理过程、审批模式、审批超时
    - 分区 header 带标题+描述、setting-card 卡片式布局
    - toggle switch 用于布尔值设置
  - **涉及文件**：index.html、css/main.css、js/app.js
  - **验证**：设置页配置项有说明、样式美观、所有字段正确绑定

- ✅ T17: 重构系统监控页 — Hermes 数据 + 趋势图
  - **问题**：无 Hermes 监控、UI 素朴
  - **修复**：
    - 添加 Hermes 监控指标区域（Gateway 状态、活跃会话数、定时任务数、技能数），使用卡片+图标布局
    - NAS 资源监控卡片样式优化（添加图标、标签头）
    - 趋势图正常显示（ECharts 主题自适应）
    - 实时刷新已有（10s 系统信息、5s 图表更新）
    - 移动端响应式适配（monitor-stats-grid）
  - **涉及文件**：index.html、css/main.css
  - **验证**：监控页显示 NAS + Hermes 数据、图表可见、数据实时更新

- ✅ T18: 重构日志查看页 — 样式 + 高亮
  - **问题**：排版差、无高亮、空间浪费
  - **修复**：
    - 日志级别颜色标签（ERROR 红、WARN 黄、INFO 蓝、DEBUG 灰）— 已有，微调尺寸
    - 关键词高亮（错误、异常、超时、error、exception、timeout、failed、critical、fatal、panic）— 使用 mark.log-highlight
    - 紧凑布局（减少 padding、gap、font-size）
    - 时间戳等宽字体 + tabular-nums
    - 工具栏布局优化（左侧筛选+右侧操作）
    - 日志计数徽章（"300 条"）
  - **涉及文件**：index.html、css/main.css、js/app.js、js/utils.js
  - **验证**：日志级别有颜色、关键词高亮、布局紧凑、计数显示

## 阶段 7: 移动端 + 收尾（P1）

- ✅ T19: 移动端响应式适配
  - **问题**：移动端布局异常
  - **修复**：
    - 新增 tablet 断点（≤900px）：模型英雄区堆叠、技能分类标签横向滚动、插件工具栏允许换行
    - 移动端（≤768px）全面适配：
      - 安全区域适配（iOS 刘海屏 env(safe-area-inset-*)）
      - 使用 100dvh 替代 100vh 解决 iOS 地址栏问题
      - 顶部栏操作按钮紧凑化（隐藏文字、仅显示图标）
      - 按钮最小触摸目标 36-44px（btn-sm、btn-primary、btn-icon）
      - 模型英雄区垂直堆叠、提供商卡片操作始终可见（触屏无 hover）
      - 渠道卡片、技能卡片、Cron 卡片紧凑间距
      - 会话详情消息头堆叠、消息间距优化
      - 日志查看器字体缩小、时间戳/级别列压缩
      - 设置页全宽输入框、设置卡片纵向堆叠
      - 所有弹窗适配（确认弹窗、插件详情、技能详情、Cron 表单）
      - Toast 全宽显示
      - 表单输入框字体 16px 防止 iOS 自动缩放
      - Toggle 开关增大触摸区域
      - 分页控件紧凑化
    - 小屏移动端（≤480px）：所有网格单列、卡片进一步紧凑、顶部栏高度缩减
    - 防止水平溢出（overflow-x: hidden）
  - **涉及文件**：css/main.css、index.html（缓存版本号更新）
  - **验证**：移动端所有页面可正常操作

- ✅ T20: 最终验收 — 全部 AC 验证
  - **验证内容**：AC1-AC14 全部通过
  - **方法**：逐条测试验收标准
  - **输出**：
    - AC1 ✅ 首次访问无弹窗，直接显示仪表盘
    - AC2 ✅ 官方控制台链接指向 http://192.168.5.110:19119/login
    - AC3 ✅ 会话列表显示 50 个会话，ID 完整可见，分页/搜索可用
    - AC4 ✅ 会话详情显示消息历史（84 条消息），有返回按钮
    - AC5 ✅ 点击编辑按钮进入 inline-edit 模式（textbox + ✓/✕ 按钮）
    - AC6 ✅ 定时任务创建按钮存在，2 个任务正确显示
    - AC7 ✅ 每个任务有编辑按钮，可修改配置
    - AC8 ✅ 每个任务有删除按钮，有确认弹窗
    - AC9 ✅ 模型配置页显示提供商下拉（mimo/minimax-cn）、模型名称输入、切换按钮
    - AC10 ✅ 消息渠道显示"飞书"中文名 + 🐦 图标 + 状态信息
    - AC11 ✅ 浅色主题默认，背景 #fafafa，文字 #171717
    - AC12 ✅ 深色主题切换正常，背景 #0a0a0a，文字 #ededed
    - AC13 ✅ 移动端元素存在（mobile-header、mobile-menu-btn、sidebar drawer）
    - AC14 ✅ 侧边栏显示 Gateway 运行状态（dot-on）+ 🔄 重启按钮
